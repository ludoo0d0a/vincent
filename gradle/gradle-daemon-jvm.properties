#Gradle Daemon JVM toolchain criteria.
#Pins the JDK that runs the Gradle daemon itself (Gradle 8.8+). With the foojay
#resolver in settings.gradle.kts, Gradle auto-provisions it if absent.
#Regenerate with: ./gradlew updateDaemonJvm --jvm-version=21
toolchainVersion=21
