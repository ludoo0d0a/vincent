{
  "name": "vin-cent-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies Gemini for the Vincent app (key off-device, App Check + Firebase ID token verified, KV quota + cache).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/wikipedia-parse.test.mjs",
    "test:e2e": "node --test test/worker-e2e.mjs test/gemini-e2e.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
