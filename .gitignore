.gradle/
build/
*/build/
.idea/
*.iml
local.properties
composeApp/google-services.json
.DS_Store
.kotlin/
captures/
node_modules/

# Signing — never commit the keystore or its credentials
*.keystore
*.jks
scripts/.keystore-credentials
scripts/.play-service-account.json
scripts/.adb-wireless

# Cloudflare Worker (AI proxy) — local build and local dev secrets
.wrangler/
worker/.dev.vars

# Generated Play release notes (from playstore/whatsnew.xml)
playstore/whatsnew/

# Locally bumped release version (managed by release tooling)
playstore/version.properties
/secrets/*.json
/composeApp/release/
/secrets/upload_certificate.pem
