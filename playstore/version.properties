# Single source of truth for the app version.
# Read by composeApp/build.gradle.kts. CI overrides via env VERSION_NAME / VERSION_CODE
# (versionCode = github.run_number; versionName from the git tag), so bump versionName
# here for non-tagged builds.
versionName=1.0.1
versionCode=110
